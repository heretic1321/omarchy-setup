# Generic workspace configuration for PC
# We don't bind workspaces to specific monitors by default to allow flexibility
# unless the user has a specific multi-monitor setup they want to enforce.

workspace = 1, default:true
